import { useState } from 'react';

export default function ImageLoader() {
  return (
    <div>
      <h3 className="font-semibold mb-2">Images</h3>
      {/* Display images without loading status */}
    </div>
  );
} 